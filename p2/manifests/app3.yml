apiVersion: apps/v1
kind: Deployment
metadata:
  name: app3-deployment 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app3 # deployment selector label. it must be the same as the pod template label and service selector label
  template:
    metadata:
      labels:
        app: app3 # pod template label. it must be the same as the deployment selector label
    spec:
      containers:
      - name: app3-container
        image: paulbouwer/hello-kubernetes:1.10
        ports:
        - containerPort: 8080
        env:
        - name: MESSAGE
          value: "Hello from app3"
---
apiVersion: v1
kind: Service
metadata:
  name: app3-service
spec:
  selector:
    app: app3 # service selector label. it must be the same as the deployment selector label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP